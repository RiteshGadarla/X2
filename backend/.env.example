# Application Environment Configuration
# Copy this file to .env and adjust as needed

APP_ENV=development
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_RELOAD=true

# Database connection string (e.g., sqlite:///./cs_agent.db or postgresql://user:pass@host/db)
DB_CON_STR=sqlite:///./cs_agent.db
